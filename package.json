{
  "name": "vibe-svgs",
  "version": "0.1.0",
  "description": "Open-source animated SVG mascot, badge, and banner library.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "serve": "bunx serve .",
    "test": "bun test tests/svg-contracts.test.ts",
    "validate": "bun run scripts/validate-svg.ts",
    "check": "bun run test && bun run validate"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
